export type CardPerformanceVariant = "existing" | "poc";

export type CardPerformanceItem = {
    bestScore: string;
    id: string;
    reactionSpeed: string;
    reviewPace: string;
    savedTerms: string;
    sequenceTitle: string;
};
